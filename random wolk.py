import


class RandomWalk
    def __init__(self, dimension, step):
        self.dim = dimension
        self.st = step
        self.matrix = /
        [[0 for step in range(self.st)] for dim in range(self.dim)]
